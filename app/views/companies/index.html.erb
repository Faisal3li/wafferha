<div class="cardSizee">
<div class="row cardSizee">
 <% @companies.each do |co| %>
    <div class="col s4">
      <div class="card">
      <div class="card-image">
<%= cl_image_tag co.image, class:"imgSize"%>
      </div>
      
        <div class="card-action">
           <%= link_to "Check offers!", company_path(co), class:"checkoffers"%>
        </div>
      </div>
    </div>
    <% end%>
  </div>
  </div>

<% if user_signed_in?  && current_user.admin%>
<%= link_to 'Add New Company', "/companies/new"%>
 <% end %>
<%#= link_to 'Add New Company', "../companies/"+@company.id.to_s+"/new"%>
 <%# end %>
